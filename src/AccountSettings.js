import React, { Component } from 'react';

class AccountSettings extends Component {
  render() {
    return (
      <div>
        <h1> Paramètres de l'application (changement nom / mdp) </h1>
      </div>
    );
  }
}

export default AccountSettings;